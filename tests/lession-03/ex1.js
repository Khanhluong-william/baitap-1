// Khai báo biến 
const Number = 100;
// Tối ưu code: Thay vì viết thêm dòng Else thì khai báo luôn ở đây
let Value = " Giá trị bạn nhập là số âm";
// Nếu số > 0 là số dương
if (Number > 0) {
    Value = "Giá trị bạn nhập là số dương"
}

console.log(Value);