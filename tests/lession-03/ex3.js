// Lặp từ 1 - 100
for ( let i = 0; i < 100; i++) {
    // nếu i chia hết cho 2 => Số chẵn
    if (i % 2 === 0) {
        console.log(`Sô ${i} là số chẵn`)
    } 
    // nếu i chia hết cho 2 => Số lẻ 
    else {
        console.log(`Số ${i} là số lẻ`)
    }
}
